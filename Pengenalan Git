* Git adalah sebuah VCS (Version Control Sytem), kalian dapat meng-ibaratkannya seperti sebuah software.
Git ini digunakan untuk mengelola segala perubahan yang terjadi pada sebuah file yang terdapat di Folder.

* Folder pada Git dinamakan Repository / Repo, (bukan folder lagi namanya).
Singkatnya Folder = Repository

* Ada juga istilah lainnya seperti commit, hash, checkout, remote, clone.
- Commit adalah segala hal yang berhubungan dengan perubahan pada file. Bisa juga di ibaratkan seperti riwayat perubahan
pada file. Mulai dari waktu (kapan filenya dirubah), isinya (kode / teks apa yang dihapus atau ditambahkan pada file).
Ini mirip sama mekanisme save-savean pada file. Bedanya, jika kalian menggunakan commit, apa yang ditambahkan dan apa yang dihapus
pasti terlihat.

- Hash adalah kode unik yang terdapat pada setiap file dan histori dari file tersebut (kalian dapat melihat hash (kode unik) tersebut, 
disamping tombol history file)

- Checkout adalah beralih atau berpindah dari satu commit ke commit lainnya (misalnya berpindah dari commit sekarang --> commit yang sebelumnya)

- Remote adalah sumber yang memiliki repository, Bisa dari Github, Bitbucket, Gitlab, dan penyedia lainnya

- Clone adalah mengambil atau menyalin seluruh isi dari sebuah repository remote ke komputer lokal

- Push adalah proses mengirimkan perubahan (file / repo) dari komputer lokal ke remote (Github, Gitlab, Bitbucket, dll)
  Push: dari komputer lokal ke remote (Github, Gitlab, Bitbucket atau lainnya)

- Pull adalah proses mengambil perubahan (file / repo) dari remote (Github, Gitlab, Bitbucket, dll) ke komputer lokal 
  Pull: dari remote (Github, Gitlab, Bitbucket atau lainnya) ke komputer lokal

* Kelebihan Git dibandingkan pengelola file yang biasanya tedapat di komputer lokal
- Git menyimpan seluruh histori / riwayat pada file
- Menggunakan Git, kalian bisa kembali ke file lama (file yang sebelumnya)
- Gak usah panik jika ada yang salah tulis (karena bisa kembali ke savean file sebelumnya)
- Online
- Bisa kolaborasi sama orang lain!. Maksudnya adalah bisa barengan ngubah isi file sama orang lain.
- Gak perlu buat file baru untuk file yang sudah dirubah isinya (karena apa yang berubah, sudah kelihatan di histori filenya)

* Perumpamaan | Git | -VS- | Pengelola (manager) file di komputer / hp kalian |
# Pengelola File:
--> 1). buat "file.txt" --> 2). buat file baru, namanya "file_1.txt" karena yang sudah ditambahkan isinya --> 3). buat file baru lagi, namanya "file_2.txt" karena ada yang ditambah dan dihapus --> 4). terdapat 3 file. Yaitu, "file.txt", "file_2.txt", "file_3.txt" dan hanya dapat melihat perubahan yang terjadi pada file secara manual.

# Git:
--> 1). buat "file.txt" --> 2). masih satu file yang sama, tapi isinya sudah ditambahkan --> 3). masih satu file yang sama, tapi ada yang ditambah dan dihapus --> 4). Tetap satu file, yaitu "file.txt" dan bisa dilihat apa saja yang dirubah pada filenya (HIJAU untuk kode / teks yang ditambahkan dan MERAH untuk kode / teks yang dihapus).

* Branch atau cabang adalah duplikasi file atau duplikasi repository dengan konten atau isi yang berbeda. Nantinya kedua file / repository ini,
dapat digabungkan menjadi 1 file / repository. Proses menggabungkan ini, disebut sebagai "merge".

# Visualisasi Branch:
--> 1). fileku.txt --> fileku.txt --> dst.
                 |
                 | --> fileku_1.txt --> dst.

* Merge adalah penggabungan 2 branch (2 cabang) atau lebih, dari file / repository, menjadi 1 file atau repository utuh.
# Visualisasi Branch:
--> 1). fileku.txt --> fileku.txt --> fileku.txt ------> |
                 |                                       |--> file_final.txt
                 | --> fileku_1.txt --> fileku_1.txt --> |

